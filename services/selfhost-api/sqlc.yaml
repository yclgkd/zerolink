version: "2"

sql:
  - engine: "postgresql"
    schema:
      - "migrations/*.sql"
    queries:
      - "internal/store/sqlc/queries.sql"
    gen:
      go:
        package: "sqlcgen"
        out: "internal/store/sqlcgen"
        sql_package: "pgx/v5"
        emit_pointers_for_null_types: true
